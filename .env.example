# Copy to .env and fill in. Server-only — the real .env is never committed.
# This default matches the local docker-compose Postgres (dev creds only).
DATABASE_URL=postgres://reg:reg_dev_pw@localhost:5432/reg_ai
